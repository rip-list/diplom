import 'package:flutter/material.dart';

class BlockDocs extends StatefulWidget {
  const BlockDocs({super.key});

  @override
  State<BlockDocs> createState() => _BlockDocsState();
}

class _BlockDocsState extends State<BlockDocs> {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(" TEST !|!"),
    );
  }
}
